apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: form-subscription
spec:
  pubsubname: reportplatform-pubsub
  topic: form-events
  routes:
    default: /api/v1/form/process
  metadata:
    rawPayload: "false"
scopes:
  - engine-reporting
