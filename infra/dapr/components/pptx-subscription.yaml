apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: pptx-subscription
spec:
  pubsubname: reportplatform-pubsub
  topic: pptx-events
  routes:
    default: /api/v1/pptx/process
  metadata:
    rawPayload: "false"
scopes:
  - processor-atomizers
  - engine-reporting
