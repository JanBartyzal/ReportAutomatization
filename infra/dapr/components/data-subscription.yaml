apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: data-subscription
spec:
  pubsubname: reportplatform-pubsub
  topic: data-events
  routes:
    default: /api/v1/data/process
  metadata:
    rawPayload: "false"
scopes:
  - engine-data
