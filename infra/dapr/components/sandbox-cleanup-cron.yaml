apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: sandbox-cleanup-cron
scopes:
  - worker
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    value: "@every 6h"  # Run 4x daily
  - name: route
    value: /jobs/sandbox-cleanup
