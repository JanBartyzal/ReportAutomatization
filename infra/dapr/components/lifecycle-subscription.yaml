apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: lifecycle-subscription
spec:
  pubsubname: reportplatform-pubsub
  topic: lifecycle-events
  routes:
    default: /api/v1/lifecycle/process
  metadata:
    rawPayload: "false"
scopes:
  - engine-reporting
