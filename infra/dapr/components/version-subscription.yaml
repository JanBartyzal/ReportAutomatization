apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: version-subscription
spec:
  pubsubname: reportplatform-pubsub
  topic: version-events
  routes:
    default: /api/v1/version/process
  metadata:
    rawPayload: "false"
scopes:
  - engine-core
